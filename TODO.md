# TODO — LonSha 记忆插件

> 只记**已确认、未修复**的项。修掉即从本文件删除，并在 CHANGELOG 里留痕。
> 不许写「待优化」这类没有判据的空条目：每条都要能回答「怎么知道它还没修」。
> 最近更新：v3.204.0
> 本版销账：T2 并行假红 —— 原记录「v348~v360 等 8 个文件依赖 /tmp/ruby-phone-work」。
> 实测三项都过期：① 路径早已指向 /home/user/ruby-phone-work（不在 /tmp）；
> ② 单独跑 17/17 全绿，并行也不红；③ 但换成活体树 /home/user/ruby-phone 后 17/17 全红 ——
> 即原「T2」不是并行问题，而是**跨仓绑定 + 依赖死快照**。本轮已退役那 17 个死桥测试，
> 并新增 scan_cross_repo_binding.mjs（P1–P5b）把「测试面不得绑本机路径/兄弟仓」变成判据。
> 另：36 个文件的本仓绝对路径也一并去绝对化。详见 CHANGELOG v3.204.0。

## T3 v3116 死代码行数上界靠手抬
`tests/v3116_dead_code.test.mjs` 的总行数上界每次新增活跃模块都要人工改（v3.202.0 从 32450 →
32700）。上界本身是对的（不给死代码留余量），但「手抬」这件事产生了漏改风险。
**怎么知道它还没修**：新增一个模块，看是否必须人工改这个上界。
方向：由「入口 + 已声明模块」的集合推导期望区间，或至少产出改写脚本 + 抬升理由模板。

## T4 九账楼层字段清单仍是手工枚举
`ledger-replay.js` 的 `LEDGER_ITEM_FLOOR_FIELDS` 是手写数组。若某本账将来新增楼层语义字段
（例如 `revealedAt` / `settledAt`），登记项不会自动跟上 —— 又是「新增忘了接线」的下一轮。
**怎么知道它还没修**：把某模块源码里的新楼层字段名与清单数组做差集，非空即未修。
方向：写一个审计脚本，扫 `*-ledger.js` / `fact-version.js` / `event-completeness.js` /
`repair-loop.js` 里出现 `Floor`/`floor` 的字段名，与清单比对并报差集。

## T5 豁免表里 `ledger` / `echo` 与 worldProg 子面的语义重叠待复核
v3.202.0 把 `ledger`（楼层账本）与 `echo`（回响池）列为豁免键，
但 `worldProg` 下已有 `echoLedger`（回声账本）与 `recallEcho`（前文回扣）——
三者是否指同一份真源、豁免是否正确，本轮未逐条实证。
**怎么知道它还没修**：读 `world-progress.js` 的 `export()` 与宿主 `this.echo` / `this.ledger` 的定义，比对字段来源。

## T6 退役面与在役面的「覆盖率转移」只做了存在性检查
v3.204.0 退役 17 个死桥测试时，逐条核对了「它们守的概念在本仓侧已有等价覆盖」
（v356 / v353 / v325 / v386），但这一步是**人工核对**，没有判据。
**怎么知道它还没修**：再退役一个文件并问「它守的东西还有别人守吗」，若答不上来就是未修。
方向：给退役登记加一列 `covered_by`，由守卫检查被引用的文件确实在在役面且确实提到同一批关键词。

## T7 `v3177` 计时断言在并行负载下偶发（一轮 4 跑中复现 1 次）
`tests/v3177_syntax_gate_perf.test.mjs` 的 A1 断言「单次全仓扫描 < 6s」。实录：一轮
`node tests/run.mjs`（并发 7）报 `v3177` exit 1 / 5103ms；随后连跑 3 轮均 EXIT 0，
手动在 6 路忙循环上重跑该文件也 EXIT 0。单独实测 `node tests/syntax-gate.mjs` 为 47–62ms，
7 路并发下最大 116ms —— 即 **A1 的 6000ms 阈值本身不是边界**，失败很可能落在该文件其他路径或单次偶发。
**怎么知道它还没修**：连跑 ≥10 轮 `node tests/run.mjs`，统计 exit≠0 的轮数与失败文件；非零即未修。
方向：先把「偶发」定住（把该文件在负载下的实际报错落盘），再决定是加串行标签还是把计时断言改成
「取多轮最小值」的稳定量。不要先改阈值 —— 阈值不是病根。
